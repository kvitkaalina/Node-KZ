const math = require('./mathModule');

const sum = math.add(5, 3);
const difference = math.subtract(10, 4);  // Используем правильное имя

console.log('Сумма:', sum);
console.log('Разность:', difference);
